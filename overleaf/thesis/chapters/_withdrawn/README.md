# Withdrawn from the thesis

Material cut from the submitted document, kept here so the decision is
recoverable. Nothing here is `\input` by `main.tex`, and
`make_upload_bundle.sh` excludes any underscore-prefixed directory, so none of
it ships to Overleaf.

| file | why | where it lives now |
|---|---|---|
| `appB-amp.tex` | The AMP/TAP bulk fixed point answers none of the six research questions; its estimand is a different problem from the one the thesis poses. | compendium |
| `appG-linear-algebra.tex` | Textbook linear algebra. | compendium |

**Not archived here**: the Gaussian identities, the ring derivations and the
Gaussian-closure proof were *moved into the body* (Chapters 3, 4 and 7
respectively), not withdrawn. No copy is kept, because a second copy of live
content is how the two come to disagree.
