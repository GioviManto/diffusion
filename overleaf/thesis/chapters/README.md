# chapters — thesis chapters and appendices

`ch01`–`ch12` plus appendices `appA`–`appG`. Inputted by `../main.tex` in order.

Figures are referenced by bare basename; `../preamble.tex` sets `\graphicspath`
to `../shared/figures/`.
