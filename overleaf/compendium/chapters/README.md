# chapters — compendium chapters

Inputted by `../main.tex`. `ch11-status` and `ch13-corrections` together are the
claim ledger: what is settled, what is provisional, what was withdrawn.

`_superseded/`, if present, holds replaced chapters kept only so the rewrite is
auditable. They are not inputted.
