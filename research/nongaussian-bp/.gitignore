outputs/_determinism/

# Agent worktrees: scratch checkouts, never part of a result.
.claude/

# Datasets: fetched per-machine, never committed. cifar-10-python.tar.gz alone
# is 163 MB, and the cluster copy is the one that matters.
data/

# overleaf/ was a generated mirror of paper/ alone. Removed 2026-08-17: it had
# gone stale (it still held the pre-rewrite paper, 1133 lines against the
# current 680) and it only ever covered one of the four documents. Replaced by
# overleaf-handover/ at the repo root, built by tools/make_handover.sh, which
# carries all four and has a --check mode for exactly this failure.
