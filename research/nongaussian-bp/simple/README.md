# simple — the minimal demonstration

A single self-contained script (`run_simple.py`) that reproduces the core result with no framework: generate a Markov chain, noise it, run BP, compare against the closed form.

For explaining the method to someone in ten minutes, and for checking that the full package has not drifted from the thing it is supposed to be doing. `SIMPLE_RESULTS.md` records what it produces.
