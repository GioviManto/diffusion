# docs — notes on the package

`PAPER_CONNECTIONS.md` maps each claim in the paper to the experiment and source file behind it. Start there when you need to know what backs a number.

The substantive write-ups are documents, not notes: see `overleaf/compendium/` for derivations and the experiment ledger.
