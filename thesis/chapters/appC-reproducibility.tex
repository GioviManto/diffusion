\chapter{Reproducibility}\label{app:repro}

\section{Repository layout}

All source material lives in a single repository; the thesis directory is
self-contained (\LaTeX{} source, figures, and the figure script are
committed together).

\begin{center}\small
\begin{tabular}{@{}ll@{}}
\toprule
Path & Content \\
\midrule
\texttt{thesis/} & this document: \LaTeX{} source, figures, figure script \\
\texttt{research/gaussian-bp/} & Gaussian chain note, code, 72-check audit \\
\texttt{research/bp-from-scratch/} & independent BP derivation and implementation \\
\texttt{research/unified-note/} & consolidated working note, code, 55-check audit \\
\texttt{research/nongaussian-bp/} & grid BP vs Gaussian-projected BP experiments \\
\texttt{research/gaussian-ar1-bp/} & Gaussian AR(1) package (BP $=$ matrix score) \\
\texttt{research/initial-experiments/} & early experiments and toy-model material \\
\bottomrule
\end{tabular}
\end{center}

\section{Audit protocol}

Every closed-form claim is verified by a deterministic check against an
independently computed reference (brute-force linear algebra, dense grid
quadrature, or finite differences), collected in audit drivers that exit
non-zero on any failure. The audit driver of
\texttt{research/gaussian-bp} runs $72$ checks:
assembly and inversion of $\Sigt$; the score--posterior route against the
matrix score; the $K = 2$ closed forms; Convention-A BP against the
matrix posterior (means, variances, scores); bulk variance, covariance
decay $q^d$, and the recombination identity; AMP mean and score
closure-independence; the AMP variance formula, existence boundary,
breakdown time, and weak-coupling law; the locality slopes; the band-fill
coefficients (with the factorial variant explicitly rejected); spectral
preservation and the large-$t$ return.
The audit driver of \texttt{research/unified-note} runs $55$ checks on
the consolidated note, including the Laplace $K = 1$ quadrature checks
and the one-step boundary-message identity.
\texttt{research/nongaussian-bp/} contains experiment drivers with logged
master-error-identity residuals ($10^{-15}$--$10^{-12}$ throughout), grid
self-convergence gates, and per-trial CSV outputs; the grid calibration
of Section~\ref{sec:l-grid} is experiment~01, the closure measurement of
Section~\ref{sec:l-closure} experiment~02. All checks were re-executed on
the build machine at the time of writing: $72/72$ and $55/55$ pass, and
all experiment gates are green.

\section{Figure-to-code map}

Every figure is regenerated by one script,
\texttt{figures/make\_figures.py}, which recomputes each panel from the
closed forms or quadrature of the corresponding chapter; the only figure
that reads stored experimental data is Figure~\ref{fig:bp-closure},
built from the per-trial CSV of \texttt{nongaussian-bp} experiment~02.
All figures are vector PDF with white background; randomness, where used,
is seeded.

\begin{center}\small
\begin{tabular}{@{}lll@{}}
\toprule
Figure & File & Source of the numbers \\
\midrule
\ref{fig:g-bandfill} & \texttt{fig\_band\_fill.pdf} & closed forms, Ch.~\ref{ch:gaussian} \\
\ref{fig:g-tridiagloss} & \texttt{fig\_tridiag\_loss.pdf} & closed forms, Ch.~\ref{ch:gaussian} \\
\ref{fig:g-locality} & \texttt{fig\_local\_vs\_full.pdf} & closed forms, Ch.~\ref{ch:gaussian} \\
\ref{fig:l-k1} & \texttt{fig\_laplace\_k1.pdf} & quadrature, Ch.~\ref{ch:laplace} \\
\ref{fig:bp-closure} & \texttt{fig\_laplace\_closure.pdf} & \texttt{nongaussian-bp} exp.~02 CSV \\
\ref{fig:bp-bulkvar} & \texttt{fig\_bulk\_variance.pdf} & closed forms, App.~\ref{app:amp} \\
\ref{fig:bp-vs-amp} & \texttt{fig\_bp\_vs\_amp.pdf} & closed forms + iteration, App.~\ref{app:amp} \\
\bottomrule
\end{tabular}
\end{center}

\section{Environment}

Python $\geq 3.10$ with NumPy, SciPy, and Matplotlib; no GPU, no neural
network framework, no stochastic training. All experiments are
deterministic given the recorded seeds; the error measures of the
locality and truncation studies are seed-free by construction
(closed-form traces). The document compiles with a single run of
\texttt{tectonic main.tex}.
